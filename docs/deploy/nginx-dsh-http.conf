# dsh.cbnac.com — HTTP only(用于 certbot ACME 验证)
server {
    listen 80;
    server_name dsh.cbnac.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
    }

    location / {
        return 301 https://dsh.cbnac.com$request_uri;
    }
}